name: dotfiles
root: ~/
windows:
  - chezmoi:
      layout: main-vertical
      panes:
        - cd ~/.local/share/chezmoi && chezmoi edit
        -  # empty
  - config:
      layout: main-vertical
      panes:
        - cd ~/.config/chezmoi && nvim ./chezmoi.toml
        -  # empty
